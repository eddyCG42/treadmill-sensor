# Python dependencies for the treadmill dashboard (Raspberry Pi).
# The dashboard server itself is pure standard library (http.server), so the
# list is deliberately tiny — only the three things that talk to hardware / net.
#
#   pip3 install -r requirements.txt --break-system-packages
#
bleak==2.1.1      # BLE — heart-rate strap (treadmill_hr.py)
pyserial==3.5     # USB serial link to the Feather (import serial)
requests==2.32.3  # Strava activity upload (treadmill_strava.py)
